#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../safe-regex2/bin/safe-regex2.js" "$@"
else
  exec node  "$basedir/../../../safe-regex2/bin/safe-regex2.js" "$@"
fi
# cmd-shim-target=/Users/matth/Developer/interior_design/interior_design/node_modules/.pnpm/find-my-way@9.7.0/node_modules/safe-regex2/bin/safe-regex2.js
