packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  esbuild: true

# esbuild ships a platform binary via its install script; allow it to build.
# (Vite and Vitest depend on esbuild.)
onlyBuiltDependencies:
  - esbuild
